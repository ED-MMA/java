package com.github.aklakina.edmma.database;

import org.hibernate.annotations.processing.HQL;

public interface Queries {
    //@HQL("where missionID = :missionID")
    //String getMissionByID(long missionID);

}
